class APIProviderConst {
  APIProviderConst._();
  static get connectTimeout => const Duration(minutes: 1).inMilliseconds;
  static get receiveTimeout => const Duration(minutes: 1).inMilliseconds;
}
